<h1>All contacts:</h1>
<% @contacts.each do |contact| %>
  <h2><%= contact.first_name + " " + contact.last_name %></h2>
  <h3><%= contact.email %></h3>
  <p><%= contact.phone_number %></p> 
<% end %>
